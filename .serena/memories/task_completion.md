# TORRE — When Task is Completed

1. Run `yarn lint` to check for ESLint errors
2. Run `yarn test` to verify tests pass
3. Update PERFORMANCE_TASKS.md if the task was a performance optimization:
   - Mark subtasks as [x]
   - Add an entry to the "Registro de cambios" table at the bottom
4. Update PROJECT_STATUS.md with what changed
5. Update HANDOFF.md if relevant for next session
6. Update CHANGELOG.md with a brief entry under the current date
